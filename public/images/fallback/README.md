# Gambar fallback

Folder ini dapat digunakan untuk poster statis atau gambar Open Graph.

Nama yang disarankan:

```text
workspace-poster.webp
portfolio-share.webp
```

Adegan 3D saat ini memiliki fallback berbasis CSS sehingga website tetap dapat digunakan meskipun folder ini masih kosong.
